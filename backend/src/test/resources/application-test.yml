spring:
  datasource:
    url: jdbc:h2:mem:paper_rabbit;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: test-only

logging:
  level:
    org.springframework.boot.autoconfigure: INFO
